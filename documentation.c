#include <stdio.h>

//ВЗИМАНЕ И ПРЕМАХВАНЕ НА ПОСЛЕДНАТА ЦИФРА
//1234
//number % 10 ----> взима последната цифра //4
//number / 10 ----> маха последната цифра //123

//ABS IN C
// 🔹 📌 Как се намира абсолютна стойност
// За ЦЕЛИ числа:
// #include <stdlib.h>
// int x = -5;
// int result = abs(x);
// 2. За ДРОБНИ числа (fabs)
// #include <math.h>
// double x = -3.14;
// double result = fabs(x);

/*-----------------------------------------------------------------------
// INPUT / OUTPUT (stdio.h)
// printf("text");          // извеждане на екран
// scanf("%d", &x);         // въвеждане от клавиатура
// getchar();               // чете 1 символ
// putchar('A');            // печата 1 символ

//STRING FUNCTIONS (string.h)
strlen(str);             // дължина на текст
strcpy(dest, src);       // копира текст
strcat(a, b);            // слепва текстове
strcmp(a, b);            // сравнява текстове

// MATH FUNCTIONS (math.h)
abs(x);                  // абсолютна стойност (int)
fabs(x);                 // абсолютна стойност (double)
pow(x, y);              // степен (x^y)
sqrt(x);                // корен
round(x);               // закръгля
floor(x);               // надолу
ceil(x);                // нагоре

//CHARACTER FUNCTIONS (ctype.h)
isdigit(c);             // дали е цифра
isalpha(c);             // дали е буква
toupper(c);             // главна буква
tolower(c);             // малка буква

//MEMORY / OTHER (stdlib.h)
malloc(size);           // заделя памет
free(ptr);              // освобождава памет
exit(0);                // край на програмата
rand();                 // случайно число
srand(time(NULL));      // seed за random

//CONVERSION
atoi(str);              // string -> int
atof(str);              // string -> float

/*---------------------------------------------------------------------

//FOR CYCLE
//📌Синтаксис
// for (инициализация; условие; промяна) {
//     // код
// }
// 🧠 Как работи
// Инициализация (1 път)
// Проверка на условие
// Изпълнение на кода
// Промяна (i++ / i--)
// 💡 Кога се използва
// когато знаеш колко пъти ще се върти цикълът
// най-често за масиви и броене
//-------------ПРИМЕР---------------------->
// int main() {
//     int i, j;
//     printf("Enter a number:");
//     scanf("%d", &i);
//     for (; i <= 10; i++, j++) {
//         printf("%d\n", i);
//     }
//     return 0;
// }

/*-----------------------------------------------------------------------

//WHILE CYCLE
//📌 Синтаксис
//while (условие) {
    // код
//}
//🧠 Как работи
// Проверява условието преди всяка итерация
// Ако е вярно → изпълнява кода
// Ако е грешно → спира
// 💡 Кога се използва
// когато не знаеш точно колко пъти ще се върти цикълът
// когато зависи от условие (вход, файл, т.н.)
//-------------ПРИМЕР---------------------->
// int main() {
//     int i;
//     printf("Enter a number: ");
//     scanf("%d", &i);
//
//     while (i < 12) {
//         printf("%d ", i);
//         i += 1;
//     }
//
//     return 0;
//}

/*-----------------------------------------------------------------------

//DO-WHILE CYCLE
// 📌 Синтаксис
// do {
//     // код
// } while (условие);
// 🧠 Как работи
// Разликата с while е много важна:
// do while първо изпълнява кода
// после проверява условието
// значи се изпълнява поне 1 път винаги
// 💡 Кога се използва
// когато искаш поне една итерация
// менюта (user input)
// validation (въвеждане на данни)
//-------------ПРИМЕР---------------------->
// int main() {
//
//     int i = 1;
//     do {
//         printf("%d ", i);
//         i++;
//     } while (i <= 5);
//
//     return 0;
// }

/*-----------------------------------------------------------------------

//SWITCH - CASE
//📌 Синтаксис
// switch (променлива) {
//     case стойност1:
//         // код
//         break;
//
//     case стойност2:
//         // код
//         break;
//
//     default:
//         // ако няма съвпадение
// }
// 🧠 Как работи
// проверява стойността на променливата
// намира съвпадащ case
// изпълнява кода
// break спира проверката
// заменя if-else
// 💡 Кога се използва
// менюта
// избори (1,2,3,4)
// дни от седмицата
// оценки
//-------------ПРИМЕР---------------------->
// int main() {
//     int day;
//
//     printf("Enter a number (1-3): ");
//     scanf("%d", &day);
//
//     switch (day) {
//         case 1:
//             printf("Monday\n");
//             break;
//         case 2:
//             printf("Tuesday\n");
//             break;
//         case 3:
//             printf("Wednesday\n");
//             break;
//         default:
//             printf("Invalid number\n");
//     }
//     return 0;
// }

/*-------------------------------------------------------------------------

//FUNCTIONS
//📌 Синтаксис
// <тип> <име> (<параметри>) {
//     // код за изпълнение
//     return <резултат от изпълнението>;
// }
//<тип> ----> Определя какъв резултат връща функцията: int – цяло число, float – дробно число, void – нищо не връща
// <параметри> -----> Това са входните данни на функцията
// 💡 Кога се използва
// правят кода по-подреден
// избягваш повтаряне
// по-лесно се намират грешки
// кодът става по-разбираем

SWAP с указатели
💡 2. Какво е swap като идея?
👉 „размяна на две стойности чрез временно съхранение“
👉 Ако функцията трябва да ПРОМЕНЯ променлива → указател
👉 Ако функцията само СМЯТА и връща → не ти трябва указател
🧠 Много проста аналогия:
x = кутия с число
p = адрес на кутията
*p = отваряш кутията и пипаш съдържанието